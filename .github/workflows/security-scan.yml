name: Security Scan

on:
  schedule:
    - cron: '0 6 * * *'  # Daily at 6am UTC
  workflow_run:
    workflows: ["Build Containers"]
    types: [completed]
    branches: [main]
  pull_request: {}
  workflow_dispatch: {}

env:
  REGISTRY: ghcr.io
  IMAGE_REPO: ghcr.io/mrdavidlaing/laingville-devcontainer-ubuntu
  USE_LOCAL_BUILD: ${{ github.event_name == 'pull_request' }}

jobs:
  trivy-scan:
    name: Trivy (Container Scanner)
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: read
      security-events: write
    strategy:
      fail-fast: false
      matrix:
        image:
          - laingville-devcontainer
          - example-node-devcontainer
          - example-node-runtime
          - example-python-devcontainer
          - example-python-runtime
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        if: env.USE_LOCAL_BUILD == 'true'
        uses: docker/setup-buildx-action@v3

      - name: Build image locally (PR)
        if: env.USE_LOCAL_BUILD == 'true'
        id: build-local
        run: |
          docker buildx build --load --target "${{ matrix.image }}" -t "local/${{ matrix.image }}:scan" .
          echo "ref=local/${{ matrix.image }}:scan" >> "$GITHUB_OUTPUT"

      - name: Log in to GHCR
        if: env.USE_LOCAL_BUILD != 'true'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Pull image (main/schedule)
        if: env.USE_LOCAL_BUILD != 'true'
        id: pull
        run: |
          REF="${{ env.IMAGE_REPO }}/${{ matrix.image }}:latest"
          docker pull "$REF"
          echo "ref=$REF" >> "$GITHUB_OUTPUT"

      - name: Set image reference
        id: image
        run: |
          if [ "${{ env.USE_LOCAL_BUILD }}" = "true" ]; then
            echo "ref=${{ steps.build-local.outputs.ref }}" >> "$GITHUB_OUTPUT"
          else
            echo "ref=${{ steps.pull.outputs.ref }}" >> "$GITHUB_OUTPUT"
          fi

      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: '${{ steps.image.outputs.ref }}'
          format: 'sarif'
          output: 'trivy-${{ matrix.image }}.sarif'
          severity: 'CRITICAL,HIGH,MEDIUM'
          vuln-type: 'os,library'
        continue-on-error: true

      - name: Upload Trivy SARIF to GitHub Security
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: 'trivy-${{ matrix.image }}.sarif'
          category: 'trivy-${{ matrix.image }}'
        continue-on-error: true

      - name: Run Trivy (table output for logs)
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: '${{ steps.image.outputs.ref }}'
          format: 'table'
          severity: 'CRITICAL,HIGH'
        continue-on-error: true

  grype-scan:
    name: Grype (Anchore Scanner)
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: read
      security-events: write
    strategy:
      fail-fast: false
      matrix:
        image:
          - laingville-devcontainer
          - example-node-devcontainer
          - example-node-runtime
          - example-python-devcontainer
          - example-python-runtime
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        if: env.USE_LOCAL_BUILD == 'true'
        uses: docker/setup-buildx-action@v3

      - name: Build image locally (PR)
        if: env.USE_LOCAL_BUILD == 'true'
        id: build-local
        run: |
          docker buildx build --load --target "${{ matrix.image }}" -t "local/${{ matrix.image }}:scan" .
          echo "ref=local/${{ matrix.image }}:scan" >> "$GITHUB_OUTPUT"

      - name: Log in to GHCR
        if: env.USE_LOCAL_BUILD != 'true'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Pull image (main/schedule)
        if: env.USE_LOCAL_BUILD != 'true'
        id: pull
        run: |
          REF="${{ env.IMAGE_REPO }}/${{ matrix.image }}:latest"
          docker pull "$REF"
          echo "ref=$REF" >> "$GITHUB_OUTPUT"

      - name: Set image reference
        id: image
        run: |
          if [ "${{ env.USE_LOCAL_BUILD }}" = "true" ]; then
            echo "ref=${{ steps.build-local.outputs.ref }}" >> "$GITHUB_OUTPUT"
          else
            echo "ref=${{ steps.pull.outputs.ref }}" >> "$GITHUB_OUTPUT"
          fi

      - name: Run Grype scanner
        uses: anchore/scan-action@v4
        id: grype
        with:
          image: '${{ steps.image.outputs.ref }}'
          fail-build: false
          severity-cutoff: high
          output-format: sarif
        continue-on-error: true

      - name: Upload Grype SARIF to GitHub Security
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: ${{ steps.grype.outputs.sarif }}
          category: 'grype-${{ matrix.image }}'
        continue-on-error: true

  syft-sbom:
    name: Syft (SBOM Generator)
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: read
    strategy:
      fail-fast: false
      matrix:
        image:
          - laingville-devcontainer
          - example-node-devcontainer
          - example-node-runtime
          - example-python-devcontainer
          - example-python-runtime
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        if: env.USE_LOCAL_BUILD == 'true'
        uses: docker/setup-buildx-action@v3

      - name: Build image locally (PR)
        if: env.USE_LOCAL_BUILD == 'true'
        id: build-local
        run: |
          docker buildx build --load --target "${{ matrix.image }}" -t "local/${{ matrix.image }}:scan" .
          echo "ref=local/${{ matrix.image }}:scan" >> "$GITHUB_OUTPUT"

      - name: Log in to GHCR
        if: env.USE_LOCAL_BUILD != 'true'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Pull image (main/schedule)
        if: env.USE_LOCAL_BUILD != 'true'
        id: pull
        run: |
          REF="${{ env.IMAGE_REPO }}/${{ matrix.image }}:latest"
          docker pull "$REF"
          echo "ref=$REF" >> "$GITHUB_OUTPUT"

      - name: Set image reference
        id: image
        run: |
          if [ "${{ env.USE_LOCAL_BUILD }}" = "true" ]; then
            echo "ref=${{ steps.build-local.outputs.ref }}" >> "$GITHUB_OUTPUT"
          else
            echo "ref=${{ steps.pull.outputs.ref }}" >> "$GITHUB_OUTPUT"
          fi

      - name: Generate SBOM with Syft (SPDX)
        uses: anchore/sbom-action@v0
        with:
          image: '${{ steps.image.outputs.ref }}'
          format: spdx-json
          output-file: 'sbom-${{ matrix.image }}-spdx.json'
        continue-on-error: true

      - name: Generate SBOM with Syft (CycloneDX)
        uses: anchore/sbom-action@v0
        with:
          image: '${{ steps.image.outputs.ref }}'
          format: cyclonedx-json
          output-file: 'sbom-${{ matrix.image }}-cyclonedx.json'
        continue-on-error: true

      - name: Upload SBOM artifacts
        uses: actions/upload-artifact@v4
        with:
          name: sbom-${{ matrix.image }}
          path: |
            sbom-${{ matrix.image }}-spdx.json
            sbom-${{ matrix.image }}-cyclonedx.json
        continue-on-error: true

  gitleaks-scan:
    name: Gitleaks (Secret Scanner)
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run Gitleaks
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        continue-on-error: true

  security-summary:
    name: Security Summary
    runs-on: ubuntu-latest
    needs: [trivy-scan, grype-scan, syft-sbom, gitleaks-scan]
    if: always()
    steps:
      - name: Generate summary
        run: |
          echo "## Security Scan Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Scanner | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|---------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| Trivy (Container) | ${{ needs.trivy-scan.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Grype (Anchore) | ${{ needs.grype-scan.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Syft (SBOM) | ${{ needs.syft-sbom.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Gitleaks (Secrets) | ${{ needs.gitleaks-scan.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Notes" >> $GITHUB_STEP_SUMMARY
          echo "- SARIF results uploaded to GitHub Security tab" >> $GITHUB_STEP_SUMMARY
          echo "- SBOM artifacts available in workflow artifacts" >> $GITHUB_STEP_SUMMARY
